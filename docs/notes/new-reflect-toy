new reflect toy example.

project a beam from the gun.
It marches until it hits,
and reflects until more hits from more planes.

Each plane can have a colour - of which is mixed by the beam

If no outer border, when hitting the outer, emit a pulse of random points.

If has border. The hit-point can add to a gradient line (rainbow example).