This is a super quick demo to present fast presentation of your theatre file.

It's standalone - compiled as a flat asset (flat.js) to from 

  http://localhost:8000/examples/scripts+theatre/emitter-sun.js/

Notes:

1. It's plain JS, so `script` include _After_ the target canvas
2. Canvas name is `playspace` by default.
