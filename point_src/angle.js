/*
A Nice angle conversion utility.

+ Angle extends Number

    a = Angle(180, 'deg')
    a.deg
    a.tau
    a.radians

    a = tau`2.1`
    // Angle(a, 'tau')
    a.degrees

functions

    d | deg | degrees
    t | tau | turn
    g | gon | grad | gradian
    r | rad | rads | radians
    h | hour
    b | binary
    compass

    rpm
 */

class Angle {
    invert() {
        // perform a 180
    }
}
